# Fenice bRain — Variáveis de Ambiente
# Copie para .env e preencha (não commitar o .env real)

# PostgreSQL local ou Supabase
DB_HOST=localhost
DB_NAME=fenice_brain
DB_USER=postgres
DB_PASS=sua_senha_aqui
DB_PORT=5432

# Para Supabase (substitua DB_HOST pelo host do projeto):
# DB_HOST=db.xxxxxxxxxxxx.supabase.co
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASS=seu_service_role_key
