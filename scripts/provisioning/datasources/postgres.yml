apiVersion: 1

datasources:
  - name: "Fenice Brain — STF"
    type: postgres
    access: proxy
    url: postgres:5432
    database: fenice_brain
    user: fenice
    password: fenice_secure_password
    isDefault: true
    editable: true
    jsonData:
      sslmode: disable
      postgresVersion: 150
    secureJsonData:
      password: fenice_secure_password
